name: Daily edition

on:
  schedule:
    # Three drops a day (IST): ~07:00, ~13:00, ~19:00. Two attempts per slot for
    # redundancy; the recent-drop guard below makes the 2nd attempt a no-op.
    - cron: "32 1 * * *"  # ~07:02 IST (01:32 UTC) morning
    - cron: "52 1 * * *"  # ~07:22 IST (01:52 UTC) morning backup
    - cron: "32 7 * * *"  # ~13:02 IST (07:32 UTC) midday
    - cron: "52 7 * * *"  # ~13:22 IST (07:52 UTC) midday backup
    - cron: "32 13 * * *" # ~19:02 IST (13:32 UTC) evening
    - cron: "52 13 * * *" # ~19:22 IST (13:52 UTC) evening backup
  workflow_dispatch:
    inputs:
      selftest:
        description: "Run a Supabase write self-test instead of curating"
        type: boolean
        default: false
      target_date:
        description: "Curate for a specific date (YYYY-MM-DD); blank = today (UTC)"
        type: string
        default: ""

jobs:
  curate:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Supabase write self-test
        if: ${{ github.event.inputs.selftest == 'true' }}
        env:
          SUPABASE_URL: https://zrjbzowohsgjbrhsldfi.supabase.co
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: |
          code=$(curl -s -o /tmp/r.txt -w "%{http_code}" -X POST "$SUPABASE_URL/rest/v1/items" \
            -H "apikey: $SUPABASE_SERVICE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_KEY" \
            -H "Content-Type: application/json" -H "Prefer: return=minimal" \
            -d '[{"section":"daily","title":"__selftest__","edition_date":"2099-01-01","is_active":false,"rank":9999,"tags":[]}]')
          echo "INSERT HTTP $code"; cat /tmp/r.txt || true
          curl -s -X DELETE "$SUPABASE_URL/rest/v1/items?edition_date=eq.2099-01-01" \
            -H "apikey: $SUPABASE_SERVICE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_KEY" >/dev/null
          echo "cleanup done"
          if [ "$code" = "201" ]; then echo "WRITE OK (service_role key works)"; else echo "WRITE FAILED — key is not service_role (got $code)"; exit 1; fi

      - name: Skip if a drop happened recently
        id: check
        if: ${{ github.event.inputs.selftest != 'true' }}
        env:
          SUPABASE_URL: https://zrjbzowohsgjbrhsldfi.supabase.co
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          EDITION_DATE: ${{ github.event.inputs.target_date }}
        run: |
          DAY="${EDITION_DATE:-$(date -u +%F)}"
          latest=$(curl -s "$SUPABASE_URL/rest/v1/items?select=created_at&edition_date=eq.$DAY&order=created_at.desc&limit=1" \
            -H "apikey: $SUPABASE_SERVICE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_KEY" | jq -r '.[0].created_at // empty')
          if [ -z "$latest" ]; then
            echo "recent=false" >> "$GITHUB_OUTPUT"; echo "no items yet for $DAY -> will add a drop"
          else
            age=$(( $(date -u +%s) - $(date -u -d "$latest" +%s) ))
            if [ "$age" -ge 12600 ]; then
              echo "recent=false" >> "$GITHUB_OUTPUT"; echo "last drop ${age}s ago (>=3.5h) -> will add a fresh drop"
            else
              echo "recent=true" >> "$GITHUB_OUTPUT"; echo "last drop ${age}s ago (<3.5h) -> skipping (recent drop)"
            fi
          fi

      - name: Install Claude Code
        if: ${{ github.event.inputs.selftest != 'true' && steps.check.outputs.recent != 'true' }}
        run: npm install -g @anthropic-ai/claude-code

      - name: Curate and add a fresh drop
        if: ${{ github.event.inputs.selftest != 'true' && steps.check.outputs.recent != 'true' }}
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          SUPABASE_URL: https://zrjbzowohsgjbrhsldfi.supabase.co
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          EDITION_DATE: ${{ github.event.inputs.target_date }}
        run: |
          claude -p "$(cat .github/scripts/daily-edition-prompt.md)" \
            --dangerously-skip-permissions \
            --max-turns 80
